{
  "buildCommand": "cd FRONTEND/estateflow && npm install && CI=false npm run build",
  "outputDirectory": "FRONTEND/estateflow/build",
  "installCommand": "cd FRONTEND/estateflow && npm install",
  "framework": "create-react-app"
}
